import React, { Component } from 'react';

import '../styles/index.scss';

export default class App extends Component {
    render() {
        return (
            <h1>React SSR with Hot Reloading</h1>
        );
    }
}